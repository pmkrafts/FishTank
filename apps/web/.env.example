# Public API URL for the Cloudflare Worker backend
PUBLIC_API_URL=https://fishtank-worker.YOUR_SUBDOMAIN.workers.dev

# FingerprintJS Pro API key (optional; empty falls back to anonymous visitor IDs)
PUBLIC_FINGERPRINT_API_KEY=YOUR_FINGERPRINT_KEY
